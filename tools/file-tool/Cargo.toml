[package]
name = "file-tool"
version = "0.1.0"
edition = "2024"
description = "WASM agent tool for file operations within sandboxed directories"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
