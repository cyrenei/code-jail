[package]
name = "http-tool"
version = "0.1.0"
edition = "2024"
description = "WASM agent tool for HTTP requests within sandboxed network boundaries"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
