# Policy for audit trail demo.
# Allows fs_read and env_read. Denies net_connect and fs_write.

[[policies]]
id = "allow-read-ops"
effect = "allow"
allowed_tools = ["fs_read", "env_read"]

[[policies]]
id = "deny-all"
effect = "deny"
